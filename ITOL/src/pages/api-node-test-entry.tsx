import React from 'react';
import ReactDOM from 'react-dom/client';
import ApiNodeTestPage from './api-node-test';
import '../App.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ApiNodeTestPage />
  </React.StrictMode>
);
